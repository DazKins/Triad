This is the Triad development repository

If you're using eclipse, this project should be runnable by just placing it in your normal workspace

If you're using any IDE other than eclipse, the project should compile the classes ok, just remember to add the "/res" folder to your build path